import { ValidEmailDirective } from './valid-email.directive';

describe('ValidEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidEmailDirective();
    expect(directive).toBeTruthy();
  });
});
